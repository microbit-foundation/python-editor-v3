/**
 * (c) 2026, Micro:bit Educational Foundation and contributors
 *
 * SPDX-License-Identifier: MIT
 */
const Tooltip = {
  baseStyle: {
    // Converged with the private theme during the RAC/Panda semantic-token
    // pre-work; the previous OSS default (Chakra's "sm") was an oversight.
    fontSize: "md",
  },
};

export default Tooltip;
